const path = require('path');

const CLIPS_FILE = path.join(__dirname, '..', 'clips.json');
const CUSTOM_COMMANDS_FILE = path.join(__dirname, '..', 'customCommands.json');

module.exports = {
  CLIPS_FILE,
  CUSTOM_COMMANDS_FILE,
};
